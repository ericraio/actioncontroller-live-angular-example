<h3>User</h3>
<p>User: <%= @user.username %></p>

<% if current_user != @user %>
  <% if current_user && current_user.follower_of?(@user) %>
    <%= link_to "Following", user_unfollow_path(@user) %>
  <% else %>
    <%= link_to "Follow", user_follow_path(@user) %>
  <% end %>
<% end %>
